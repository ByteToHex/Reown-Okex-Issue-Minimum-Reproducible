using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum SupportedWallets {
    metamask, // Same as Book's platform name
    okex,
    trust,
    tokenpocket // Same as Book's platform name
}
